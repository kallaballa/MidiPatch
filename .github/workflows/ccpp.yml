name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    with:
        submodules: true
    - name: prepare
      run: sudo ./prep_ubuntu.sh
    - name: make
      run: make
